$(function(){
	
	$('.top-slider__inner').slick({
		dots: true,
		arrows: false,
		// Плавное переключения
		fade: true,
		// Автоматически переключения
		autoplay: true,
		// Скорость переключения
		autoplaySpeed: 3000,
	});

});